import 'package:flutter/widgets.dart';

class FlutterNostrFeedWidget extends StatelessWidget {
  const FlutterNostrFeedWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: null);
  }
}
